<Window x:Class="ProjectCuoiKy.TakeTest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectCuoiKy"
        mc:Ignorable="d"
        Title="TakeTest" Height="1080" Width="1920"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid Background="LightGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.1*" />
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="0.1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="0.3*" MinHeight="75" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" MinHeight="200"/>
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <!-- Hiển thị thông tin bài kiểm tra -->
        <Grid Grid.Column="1" Grid.Row="1" Background="White">
            <StackPanel Orientation="Vertical" Margin="10">
                <TextBlock FontWeight="Bold" Text="Test Information:" Margin="0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Test Name:" Width="100"/>
                    <Label x:Name="lblTestName" Content="--" Width="200"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="Due Time:" Width="100"/>
                    <Label x:Name="lblDueTime" Content="--" Width="200"/>
                </StackPanel>
                <Button Grid.Column="0" Name="btnSubmit" Content="Submit Test" Width="150" Height="40" VerticalAlignment="Top" HorizontalAlignment="Left" Click="btnSubmit_Click"/>
            </StackPanel>
        </Grid>

        <!-- Câu hỏi và câu trả lời -->
        <Grid Grid.Column="1" Grid.Row="3" Background="White">
            <TextBlock Text="Questions:" Margin="10,20,0,0"/>
            <ListBox x:Name="questionsList" Margin="10,40,10,10" SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Name="tblQuestionText" Text="{Binding QuestionText}" FontWeight="Bold"/>

                            <RadioButton Content="{Binding A}" Tag="A" GroupName="{Binding QuestionId}" Margin="5,0" Checked="RadioButton_Checked"/>
                            <RadioButton Content="{Binding B}" Tag="B" GroupName="{Binding QuestionId}" Margin="5,0" Checked="RadioButton_Checked"/>
                            <RadioButton Content="{Binding C}" Tag="C" GroupName="{Binding QuestionId}" Margin="5,0" Checked="RadioButton_Checked"/>
                            <RadioButton Content="{Binding D}" Tag="D" GroupName="{Binding QuestionId}" Margin="5,0" Checked="RadioButton_Checked"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
    </Grid>
</Window>
